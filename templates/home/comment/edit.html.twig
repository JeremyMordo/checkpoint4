{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}
    <div class="row d-flex flex-column align-items-center">
        <h1 class="col-8 text-center">Modifier un commentaire :</h1>
        {{ include('home/comment/_form.html.twig', {'button_label': 'Update'}) }}
        <a class="underline my-3" href="{{ path('comment_index') }}">Revenir à la liste des commentaires</a>
    </div>
{% endblock %}
